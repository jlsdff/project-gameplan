import React, { useMemo, useState } from 'react';
import { useReactTable } from '@tanstack/react-table';

const StatTable = ({ id, players}) => {
  return <div>TABLE HERE</div>
}

export default StatTable;